export enum Crud {
  GET = "GET",
  POST = "POST",
  PUT = "PUT",
  DELETE = "DELETE",
  PATCH = "PATCH"
}

export enum CollectionType {
  Single,
	EP,
	Album,
	Playlist
}

export enum ElementType
{
	Song,
	Collection,
	Artist
}